{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Enable project references for faster builds
    "composite": true,
    "rootDir": "./src",
    "outDir": "./dist",

    // Override noEmit for this package since we build it
    "noEmit": false,

    // Generate declaration files
    "declaration": true,
    "declarationMap": true,

    // Enforce proper CJS import style for Phaser
    "esModuleInterop": false,
    "allowSyntheticDefaultImports": false,
    "skipLibCheck": false,

    // JSX configuration
    "jsx": "react-jsx",
    "jsxImportSource": ".",

    // Include Node types for scripts
    "types": ["node"],

    // TS 6: keep legacy options temporarily while migrating config
    "ignoreDeprecations": "6.0"
  },
  "include": ["src/**/*"],
  "exclude": ["src/samples/**/*", "node_modules", "dist", "**/*.spec.ts", "**/*.test.ts"]
}
