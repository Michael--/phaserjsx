{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // No composite mode for apps
    "composite": false,
    "rootDir": "./src",

    // Include Vite client types
    "types": ["vite/client"],

    // JSX configuration
    "jsx": "react-jsx",
    "jsxImportSource": "@number10/phaserjsx",

    // TS 6: keep legacy baseUrl paths behavior during migration
    "ignoreDeprecations": "6.0"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "**/*.spec.ts", "**/*.test.ts"],
  "references": [
    {
      "path": "../../packages/ui"
    }
  ]
}
