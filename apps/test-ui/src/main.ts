// @phaserjsx/test-ui app entry point
// This file will be populated with actual Phaser game setup later

const app = document.getElementById('app')
if (app) {
  app.innerHTML =
    '<h1>Hello, PhaserJSX Test UI!</h1><p>This is a function test to verify the build structure works.</p>'
}
